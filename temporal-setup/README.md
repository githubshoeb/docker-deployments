Local temporal Evironment Setup Steps For Docker
======================================================

1. Installing temporal Locally:
----------------------------------------------
Go into the linux directory and Open command prompt or terminal at temporal folder
```
docker compose build --no-cache 
docker compose up -d
docker compose ps
```