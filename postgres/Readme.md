Local postgres Evironment Setup Steps For Docker
======================================================

1. Installing postgres Locally:
----------------------------------------------
Go into the linux directory and Open command prompt or terminal at postgres folder
```
docker compose build --no-cache 
docker compose up -d
docker compose ps
```